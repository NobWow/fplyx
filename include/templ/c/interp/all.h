/* c_interp/all.h - contains all the instructions to create C code for all platforms
 * Template interpreters are very special kind of interpreters that aren't evaluating the code, but translates them.
 * They require an output data, a memory buffer, a file etc.
 * Only template devices could be attached to them
 */
